# apps/admin

Internal administration (`admin.`). Separate origin, separate auth realm, separate RBAC.
**Never a route inside the dashboard** — see ADR-010.
Scaffolded by **CORE-017**. Empty until then.
