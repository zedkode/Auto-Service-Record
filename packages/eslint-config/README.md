# @autoservices/eslint-config

Shared flat ESLint config, including the guard rules that enforce documented security
invariants (Resend SDK containment, cross-app imports, `$unscoped` restriction).
**These are controls, not style preferences.** Built by **CORE-003**.
