# @autoservices/email

`EmailService`, `MailTransport` implementations and templates.
**Nothing outside `src/transports/resend.transport.ts` may import the Resend SDK.**
See `EMAILS.md` and ADR-006. Built by **CORE-014**.
